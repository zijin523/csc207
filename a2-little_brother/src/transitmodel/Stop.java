package transitmodel;

/**
 * An Stop class that represents a bus stop.
 */
public class Stop extends Route {

    /**
     * Initialize a bus stop with its name and type.
     *
     * @param name Stop Name
     * @param type Stop Type
     */
    public Stop(String name, String type) {
        super(name, type);
    }
}
